import Navigate from "@/lib/Navigate";
import "./globals.css";

export const metadata = {
  title: "Actmedia Monitor",
  description: "Generated by Actmedia Monitor",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <Navigate />
        {children}
      </body>
    </html>
  );
}
